<?xml version="1.0"?>
<!DOCTYPE suppressions PUBLIC
        "-//Checkstyle//DTD SuppressionFilter Configuration 1.2//EN"
        "https://checkstyle.org/dtds/suppressions_1_2.dtd">

<suppressions>
    <suppress checks="LineLength" files="package-info\.java"/>
    <!-- Klasy @SpringBootApplication i test-launchery mają tylko main() → Checkstyle widzi "utility
         class", ale prywatny konstruktor zepsułby Spring Boot. Wyłączamy regułę dla *Application. -->
    <suppress checks="HideUtilityClassConstructor" files=".*Application\.java"/>
    <suppress
            checks="FileLength"
            files=".*/modules/.*/infrastructure/web/.*(Command|Query)Controller\.java"/>

    <!-- Testy używają idiomu `static final ArchRule|JavaClasses|ApplicationModules <lowercaseName>`
         (ArchUnit @ArchTest, Spring Modulith ApplicationModules, fixtures) — pól static final, które
         ConstantName traktuje jak stałe i żąda UPPER_SNAKE. To kanoniczny styl w testach, więc
         wyłączamy ConstantName wyłącznie w src/test. W src/main reguła pozostaje twarda. -->
    <suppress checks="ConstantName" files="[/\\]src[/\\]test[/\\]"/>

</suppressions>
