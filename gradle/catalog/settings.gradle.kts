// Publikowany version catalog — jedyne źródło prawdy wersji bibliotek/pluginów dla 3 repo.
// Konsumpcja: dependencyResolutionManagement { versionCatalogs { create("libs") { from("pl.seniordeveloper:platform-catalog:1.0.0") } } }
rootProject.name = "platform-catalog"
